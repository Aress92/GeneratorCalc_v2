--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -8,7 +8,7 @@
 
 **Architecture**: Next.js 14 frontend → FastAPI backend → Celery workers → MySQL database → Redis cache/queue
 
-**Current Status**: **ALL MVP MODULES COMPLETED** ✅ - Import mo...e coverage baseline. **System ready for production deployment**.
+**Current Status**: **MVP Complete – Pre-Production** – baseline test coverage 45% (target 80%); known ORM relation fix pending; readiness for UAT.
 
 **Quick Start**:
 1. `docker compose up -d` (starts all services)
@@ -654,6 +654,9 @@
 3. **Use Centralized API Client**: All API calls MUST use classes from `/lib/api-client.ts`
 
 #### Known Issues (Current Status)
+
+- **Frontend base URL**: Ensure `NEXT_PUBLIC_API_URL` points to the public entry (Nginx host), not the Docker service name (`backend`).
+- **SSE via Nginx**: `proxy_buffering off; proxy_cache off;` and `text/event-stream` must be honored; use a dedicated `location` for `/api/v1/jobs/*/events`.
 - **ReportTemplate.generated_reports**: Relationship commented ... needs template_id column in Report model
 - **Celery healthcheck**: Workers may show "unhealthy" status but function correctly (known Docker Compose issue)
 - **Test coverage**: 45% current vs 80% target - focus on service layer testing
@@ -709,6 +712,8 @@
 
 ## Current System Status (Updated 2025-09-27)
+**System Status**: **MVP Complete – Pre-Production** – UAT pending; do not market as production until test coverage ≥80% and SSE/nginx verified.
+
 
 **Overall Progress**: 100% MVP Complete ✅
