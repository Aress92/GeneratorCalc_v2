--- a/RULES.md
+++ b/RULES.md
@@ -565,7 +565,8 @@
       setError(null);
       
       // Start optimization job
-      const response = await fetch('/api/v1/optimize', {
+      const base = (process.env.NEXT_PUBLIC_API_URL || '').replace(/\/$/, '');
+      const response = await fetch(`${base}/api/v1/optimize`, { credentials: 'include',
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({ scenario_id: scenarioId })
@@ -576,7 +577,8 @@
       const { job_id } = await response.json();
       
       // Setup SSE connection for progress
-      const es = new EventSource(`/api/v1/jobs/${job_id}/events`);
+      const base = (process.env.NEXT_PUBLIC_API_URL || '').replace(/\/$/, '');
+      const es = new EventSource(`${base}/api/v1/jobs/${job_id}/events`, { withCredentials: true });
       setEventSource(es);
@@ -669,7 +671,8 @@
         login: async (credentials) => {
           set({ isLoading: true });
           try {
-            const response = await fetch('/api/v1/auth/login', {
+            const base = (process.env.NEXT_PUBLIC_API_URL || '').replace(/\/$/, '');
+            const response = await fetch(`${base}/api/v1/auth/login`, { credentials: 'include',
               method: 'POST',
               headers: { 'Content-Type': 'application/json' },
               body: JSON.stringify(credentials)
@@ -686,7 +689,8 @@
           }
         },
         
-        logout: async () => {
-          await fetch('/api/v1/auth/logout', { method: 'POST' });
+        logout: async () => {
+          const base = (process.env.NEXT_PUBLIC_API_URL || '').replace(/\/$/, '');
+          await fetch(`${base}/api/v1/auth/logout`, { method: 'POST', credentials: 'include' });
           set({ user: null });
         },
         
@@ -692,7 +696,8 @@
         checkAuth: async () => {
           try {
-            const response = await fetch('/api/v1/auth/me');
+            const base = (process.env.NEXT_PUBLIC_API_URL || '').replace(/\/$/, '');
+            const response = await fetch(`${base}/api/v1/auth/me`, { credentials: 'include' });
             if (response.ok) {
               const user = await response.json();
               set({ user });
@@ -1027,0 +1032,19 @@
+### CI Guard: Ban relative API calls
+Add the following ESLint rule (or pre-commit grep) to fail builds if someone uses a relative `/api/...` URL from the frontend:
+
+```js
+// .eslintrc.cjs
+module.exports = {
+  rules: {
+    'no-restricted-syntax': [
+      'error',
+      {
+        selector: "CallExpression[callee.name='fetch'] Literal[value^='/api/']",
+        message: 'Do not use relative /api URLs. Use NEXT_PUBLIC_API_URL (absolute) or ApiClient.'
+      }
+    ]
+  }
+}
+```
